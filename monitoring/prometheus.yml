global:
  scrape_interval: 30s
  evaluation_interval: 30s

scrape_configs:
  - job_name: api-semit
    metrics_path: /metrics
    static_configs:
      - targets: ['api:5000']
        labels:
          service: api-semit
