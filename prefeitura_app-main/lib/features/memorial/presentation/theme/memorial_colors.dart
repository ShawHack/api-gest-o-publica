import 'package:flutter/material.dart';

/// Identidade visual do Memorial (espelho da web).
class MemorialColors {
  MemorialColors._();

  static const Color primary = Color(0xFF364BA3);
  static const Color primaryDark = Color(0xFF12303A);
  static const Color background = Color(0xFFF5F6FA);
  static const Color cardText = Color(0xFF12303A);
  static const Color muted = Color(0xFF6B7280);
}
