// Stub para FormsGarcaScreen em plataformas não-web
// Esta tela só está disponível na web
import 'package:flutter/material.dart';

class FormsGarcaScreen extends StatelessWidget {
  const FormsGarcaScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Forms Garça')),
      body: const Center(
        child: Text('Esta tela só está disponível na versão web'),
      ),
    );
  }
}







