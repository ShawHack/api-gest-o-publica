// Stub para FormsGarcaLoginScreen em plataformas não-web
// Esta tela só está disponível na web
import 'package:flutter/material.dart';

class FormsGarcaLoginScreen extends StatelessWidget {
  const FormsGarcaLoginScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Login Forms Garça')),
      body: const Center(
        child: Text('Esta tela só está disponível na versão web'),
      ),
    );
  }
}







