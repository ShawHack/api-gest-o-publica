const fs=require('fs');const{validateAndScan}=require('../helpers/comtur-upload');const{recordAudit}=require('../helpers/audit-service')
module.exports=class C{static async upload(req,res){if(!req.file)return res.status(422).json({error:'Arquivo obrigatório'});try{const data=await validateAndScan(req.file);await recordAudit(req,{action:'comtur.media.upload',module:'comtur',resourceType:'comtur_media',eventType:'UPLOAD',metadata:{url:data.url,hash:data.hash,mimeType:data.mimeType,sizeBytes:data.sizeBytes}});res.status(201).json({data})}catch(e){try{fs.unlinkSync(req.file.path)}catch(_){}res.status(e.status||500).json({error:e.message||'Falha no upload'})}}}
