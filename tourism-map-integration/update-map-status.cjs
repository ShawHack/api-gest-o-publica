const fs = require('fs')
const file = process.argv[2]
let text = fs.readFileSync(file, 'utf8')
for (const step of [
  'Inventariar e exportar os 41 pontos, fotos e categorias do mapa legado; registrar contagens e inconsistências sem alterar dados.',
  'Ampliar o conteúdo turístico com localização estruturada, coordenadas, origem/migração e configuração QR; manter compatibilidade com conteúdos COMTUR existentes.',
  'Criar migração idempotente: simulação obrigatória, importação inicial como `draft`, chave de origem única e relatório de itens ignorados/conflitantes.',
  'Criar página pública estável `/turismo/local/{slug}` e API pública por slug; QR nunca deve apontar para ObjectId ou URL administrativa.',
  'Incorporar mapa ao portal Turismo Garça usando somente locais publicados com coordenadas válidas; filtros e lista acessível devem funcionar sem depender apenas do mapa visual.',
]) text = text.replace(`[ ] ${step}`, `[x] ${step}`)

const marker = '**Estado inicial verificado:** mapa legado funcional em `/mapaturistico/`, 41 pontos ativos e administração exclusiva de `admin`. Portal `/turismo/` funcional, mas catálogo público sem atrativos; banco COMTUR contém 2 conteúdos de governança. O mapa ainda é acessado por link externo no portal novo. O formulário `qr_point` é genérico e não deve ser usado como cadastro paralelo.\n'
const result = `\n**Execução em 15/09/2026:** inventário JSON e cópia dos arquivos foram guardados em \`/home/semit/Documentos/deploy-backups/tourism-map-20260915/\`. A simulação encontrou 41 pontos aptos, zero conflitos e zero avisos. A migração criou 41 registros exclusivamente como \`draft\`; uma segunda simulação encontrou zero criações e 41 itens já reconhecidos, comprovando idempotência. O legado permaneceu com 41 pontos e continua público.\n\nA API recebeu consulta cartográfica pública \`GET /api/comtur/map/locations\`, limitada a locais publicados com coordenadas válidas, resolução controlada do ID legado e administração do ciclo físico de QR vinculada ao próprio local. A API voltou \`healthy\`; enquanto os registros aguardam revisão, o endpoint público retorna lista vazia por desenho de segurança. Nenhum ponto foi publicado automaticamente e nenhum QR foi ativado.\n\n**Pendências para concluir:** revisar e publicar gradualmente os locais; criar a interface visual de mapa/lista no portal; confirmar e implantar a ficha \`/turismo/local/{slug}\`; especializar o formulário administrativo e gerar PNG/PDF acessível; somente então trocar o link e ativar redirecionamentos do legado.\n`
if (!text.includes('**Execução em 15/09/2026:** inventário JSON')) text = text.replace(marker, marker + result)
text = text.replace('A API recebeu consulta cartográfica pública `GET /api/comtur/map/locations`, limitada a locais publicados com coordenadas válidas, resolução controlada do ID legado e administração do ciclo físico de QR vinculada ao próprio local.', 'A API recebeu consulta cartográfica pública `GET /api/comtur/map/locations`, limitada a locais publicados com coordenadas válidas, resolução controlada do ID legado e administração do ciclo físico de QR vinculada ao próprio local. Foram implantadas a ficha estável `/turismo/local/{slug}` e a página `/turismo/mapa/`, com busca, categorias, marcadores e lista acessível. O botão “Ver mapa” do portal passou a usar a URL nova; durante a revisão, a página oferece acesso explícito ao legado.')
text = text.replace('**Pendências para concluir:** revisar e publicar gradualmente os locais; criar a interface visual de mapa/lista no portal; confirmar e implantar a ficha `/turismo/local/{slug}`; especializar o formulário administrativo e gerar PNG/PDF acessível; somente então trocar o link e ativar redirecionamentos do legado.', '**Pendências para concluir:** revisar e publicar gradualmente os 41 locais; especializar o formulário administrativo e gerar PNG/PDF acessível; validar papéis administrativos com contas autorizadas; somente então ativar os redirecionamentos individuais do legado. O legado não deve ser removido durante a homologação.')
fs.writeFileSync(file, text)
